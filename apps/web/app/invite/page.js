"use client";
import {useState} from "react";
export default function InvitePage(){const [p,setP]=useState("");const [c,setC]=useState("");const [e,setE]=useState("");async function go(x){x.preventDefault();const token=location.hash.slice(1);if(!token||p!==c){setE("Tautan atau password tidak valid.");return}const r=await fetch("/api/v1/auth/dashboard-invites/accept",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,password:p})});if(!r.ok){setE("Undangan kedaluwarsa atau password terlalu lemah.");return}location.hash="";location.href="/"}return <main className="auth-page"><section className="auth-card"><span className="eyebrow">DASHBOARD INVITE</span><h1>Aktifkan akun</h1><p>Buat password minimal 12 karakter.</p><form onSubmit={go}><input type="password" minLength="12" value={p} onChange={x=>setP(x.target.value)} required/><input type="password" minLength="12" value={c} onChange={x=>setC(x.target.value)} required/><button>Aktifkan dan masuk</button></form>{e&&<p className="notice error">{e}</p>}</section></main>}
